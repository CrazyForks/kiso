["The three modules under src/ each carry their own copy of the same record-validation block. Extract it so the validation exists in exactly one place and the three modules use it. This is a refactor with a constraint: BEHAVIOUR MUST NOT CHANGE. Every error message must stay byte-identical, including the record index it names, and every entry point must keep its current name and signature. Read all three files before you move anything — they are copies, but confirm that rather than assume it. `node tests/records.test.js` must still pass, and do not change the tests."]
