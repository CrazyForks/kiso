# 0.42.3 — release report (PUBLISHED)

One housekeeping PR (#155) and one release PR, merged through the
protected-branch flow with the required `check` green. The owner's
word, 2026-09-26: remove every private repository name from the tree
and release.

- **Tag** `v0.42.3` (annotated) on `main` `15c34d12` (the release PR #156).
- **Published** from the 18 tarballs the release smoke ran on: a repack
  of the merged `main` was byte-identical to them (18 sha256, compared).
- **Established on the write path:** a second publish of each of the 18
  was refused as already published (18/18). The read side lagged longer
  than usual (a first install loop of 28 attempts, 20 s apart, gave up
  on one package still unserved; nothing was re-published; a second
  loop installed on its 3rd attempt).
- **The owner's machine runs the PUBLISHED artifact:** `kiso --version`
  → 0.42.3; the 14 packages in its closure and all 18 global siblings
  read back at 0.42.3; **the installed dists were grepped for the
  removed names: 0 files.**
- **GitHub release:** https://github.com/vincemakes/kiso/releases/tag/v0.42.3
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.42.2`.
- **Lockstep:** no code change → a PATCH.

## What is in it

| PR | what changed |
|---|---|
| #155 | Comments, ADRs, plans, evaluation fixtures, bench reports, findings and test fixtures no longer name any private repository; fixtures were changed with same-length tokens so every width and byte count they pin is unchanged. No code change; the kernel line count and every request byte are as in 0.42.2 |

## Bench (BM-1 §3)

No code moved; the byte fixture and the size gate say so in `check`.
The release smoke is the non-regression half on the release artifact,
and this release carries one gate of its own: **every one of the 18
packed tarballs was unpacked and grepped for the removed names before
the leg — 0 files.**

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0423-smoke-2026-09-26.md`): one kiso T5 leg on the packed
0.42.3 candidate (18 tarballs from the release branch's bump commit,
every package rebuilt from the merged main first, installed isolated,
sha256 recorded before the leg). Validity and every gate green — verify
pass (wall 73 s), 23 decisions all bypass-approved, 0 compactions, none
of the four notices, one session and one trace; 41 trace lines all
validate at schema 7, no request failed, nothing credential-shaped.
`cost_weighted` **26,138.0** over 31 requests, 0 unknown: **inside the
rc range (20,907.0–36,624.0) — IN BAND** (0.42.2: 26,015.2). Raw leg:
kiso-doc `bench-archives/rel-0423-smoke.tgz`, sha256 `b1b1e5d9…28b2996`.

## Evidence

- `npm run check` on #155's head `bb4ee5a2`: unit 369 files / 3,541
  tests, PTY 183 / 853, the packed smoke and the demo PASS (exit 0) —
  the exit read before the PR was opened.
- CI `check` green on #155 and the release PR #156.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass (published 0.42.3, a real model)

- Plain `kiso`, profile `ds`, mode `bypass`. Session `dogfood-0423` on a
  fresh clone of the ledger fixture at its baseline.
- The same task as 0.40.3–0.42.2: `npm test` 4/4, 12 tool calls
  (list_dir 2, search_text 1, read_file 5, edit_file 3, shell 1) over 6
  requests, 14 s, `fresh 2k out 1.6k · cache 92%`; every one of the 12
  decisions `mode:bypass`; no tool failed; 0 compactions; the terminal
  `completed`; `/status` ends with `version 0.42.3`. The diff is right
  (3 files, +14 −8).

### The owner's pass

`bench/rd1/0.42.3-dogfood-checklist.md`.
