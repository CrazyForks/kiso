# 0.42.1 — release report (CANDIDATE)

One fix PR (#147) and one release PR, merged through the protected-branch
flow with the required `check` green. The owner's word, 2026-09-25:
"可以开" (open the patch) for the products' requests #14 and #15.

- **Tag** `v0.42.1` (annotated) on `main` — filled in at publish.
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.42.0`.
- **Lockstep:** fix-only → a PATCH.

## What is in it

Two composition seams the products asked for after adopting 0.42.0:

| PR | what changed |
|---|---|
| #147 | **#14 (reelfo):** a function append may return `undefined` — no append this attempt, no separator; the extension contract now admits it (`append: string | (() => string \| undefined)`). `""` stays a real, empty append that still joins |
| #147 | **#15 (uooki):** a session `systemPrompt` of `""` had composed as `"" + "\n\n" + table` (and before the appends) — two model-visible newlines at the top; `runBasePrompt` and `joinPrompt` now compose `""` as absent |

## Bench (BM-1 §3)

kiso-code has a non-empty base prompt and string appends; the byte
fixture (`apps/cli/tests/tool-rules-byte-fixture.test.ts`) held — no
request byte of kiso-code moved. The release smoke is the
non-regression half on the release artifact.

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0421-smoke-2026-09-25.md`): one kiso T5 leg on the packed 0.42.1
candidate (18 tarballs from the release branch's bump commit, installed
isolated, sha256 recorded before the leg). Validity and every gate green
— verify pass (wall 63 s), 23 decisions all bypass-approved, 0
compactions, none of the four notices, one session and one trace; the
trace's 42 lines all validate (32 request records at schema 7, nothing
credential-shaped). **One request failed with a network termination
(`[deepseek] request failed: terminated`) and was retried; the run
completed; its usage is unknown (1 of 32).** `cost_weighted`
**26,317.4** over 32 requests, 1 unknown: **inside the rc range
(20,907.0–36,624.0) — IN BAND** (0.42.0: 21,220 over 30; in-band
variance on the same bytes). Raw leg: kiso-doc
`bench-archives/rel-0421-smoke.tgz`, sha256 `72291db8…8342b2eb3`.

## Evidence

- `npm run check` on #147's head `be0cc170`: unit 368 files / 3,537
  tests, PTY 183 / 853, the packed smoke and the demo PASS (exit 0). The
  first check on the PR's first commit had FAILED at typecheck (the task
  extension's test read the append as a string) and the PR had been
  opened with auto-merge armed before that exit was read — disarmed,
  fixed, re-checked, recorded in the PR.
- CI `check` green on #147 and the release PR.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass — after the publish

_filled in_

### The owner's pass

`bench/rd1/0.42.1-dogfood-checklist.md`.
