# 0.41.2 — release report (PUBLISHED, APPROVED)

One fix PR (#135) and one release PR, merged through the protected-branch
flow with the required `check` green. The owner's word, 2026-09-24:
"release 0.41.2; once it is green, release".

- **Tag** `v0.41.2` (annotated) on `main` `1705a277` (the release PR #136).
- **Published** from the 18 tarballs the release smoke ran on (both legs):
  a repack of the release branch after merging main, and a repack of the
  merged `main` itself, were each byte-identical to them (18 sha256).
- **Established on the write path:** a second publish of each of the 18
  was refused as already published (18/18).
- **The owner's machine runs the PUBLISHED artifact:** `kiso-code`
  installed globally at 0.41.2 (on the 16th attempt, 20 s apart, while
  the registry's read side caught up); `kiso --version` → 0.41.2; the 14
  packages in its closure read back at 0.41.2 both nested under it and
  as the global tree's siblings.
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.41.1`.
- **Lockstep:** fix-only → a PATCH.

## What is in it

Two requests from a host's adoption map (the host session's own
responsibility map, 2026-09-24), both in `packages/server`:

| PR | what changed |
|---|---|
| #135 | **the transport accepts every session id the store does.** It had required a leading letter or digit; the store's rule (`^[A-Za-z0-9._-]+$`) never did, and a nanoid starts with `_` or `-` about 1 time in 32 — every such session answered 404. Now the store's rule with a length cap |
| #135 | **`hooks.onSettled`** — after a run's terminal has been delivered and the session is idle: the terminal's kind, the uncertain executions still awaiting a verdict, the high-water seq. The product's "what next" (a follow-up turn, a resume at zero, a notification); the service still never starts a run on its own |
| #135 | the README's shutdown order that keeps every run resumable (drain → stop the listener → `store.closeAll()` → exit), why `close()` is not that, and not to call `agent.close()` over a shared store |

## Bench (BM-1 §3)

Everything is in `packages/server`; kiso-code imports none of it, and no
request byte moves (the byte fixture blocks). The release smoke is the
non-regression half on the release artifact.

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0412-smoke-2026-09-24.md`): one kiso T5 leg on the packed 0.41.2
candidate. **Leg 1 is VOID:** the machine's name resolution went away
mid-leg — runs 6–8 ended `code: network` (`the host name did not
resolve`), verify fail, wall 287 s; it stays in the record (kiso-doc
`bench-archives/rel-0412-smoke.tgz`). The owner: "try again". **Leg 2**
on the same installed artifact, run id 2, after the endpoint answered
again: validity and every gate green — verify pass (wall 60 s), 22
decisions all bypass-approved, 0 compactions, none of the four notices,
one session and one trace; the trace's 40 lines all validate (30
request records at schema 7, no request failed, nothing
credential-shaped). `cost_weighted` **21,777.8** over 30 requests, 0
unknown: **inside the rc range (20,907.0–36,624.0) — IN BAND.** Raw leg:
kiso-doc `bench-archives/rel-0412-smoke-leg2.tgz`, sha256
`9405b3c4…a6b73847`.

## Evidence

- `npm run check` on #135's head: unit 367 files / 3,524 tests, PTY 183
  / 853, the packed smoke and the demo PASS (exit 0).
- CI `check` green on #135 and the release PR.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass (published 0.41.2, a real model)

- Plain `kiso`, profile `ds`, mode `bypass`. Session `dogfood-0412` on a
  fresh clone of the ledger fixture at its baseline.
- The same task as 0.40.3–0.41.1: `npm test` 4/4, 11 tool calls over 5
  requests, 12 s, `fresh 2k out 1.8k · cache 91%`; every one of the 11
  decisions `mode:bypass`; no tool failed; the terminal `completed`;
  `/status` ends with `version 0.41.2`. Three `edit_file` calls, one per
  module, none refused; the diff is right.

### The owner's pass

`bench/rd1/0.41.2-dogfood-checklist.md`. The owner ran it on 2026-09-24:
"dogfood passed, APPROVED".

## Status: APPROVED (2026-09-24)

Both dogfood passes are in the record.
