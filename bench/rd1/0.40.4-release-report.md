# 0.40.4 — release report (CANDIDATE)

One review PR and one release PR, merged through the protected-branch
flow with the required `check` green. The owner's word, in the worker's
session on 2026-09-23: "continue in this order; after the merge, release
0.40.4".

- **Tag** `v0.40.4` (annotated) on `main` — filled in at publish.
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.40.3`.

## What is in it

| PR | what changed |
|---|---|
| profile headers | a model profile names the headers its endpoint needs (`"headers"`); `{session}` in a value becomes the kiso session's id, so one conversation keeps one id across `/model` and `/resume` and two never share one; credential and framing headers are refused by name (a key never lives in the config file); the three adapters send them (the Responses adapter merges its own credential over them). A gateway that refuses requests without its session header is now reached directly, without a local forwarder |

## Bench (BM-1 §3 — the provider tier)

The request body never changes, and a profile with no `headers` builds
options identical to 0.40.3's (pinned); the bench profile configures none.
The contract gates: the parse, the expansion, the three adapters on the
wire against a loopback (with a mutation check), and the built CLI end to
end (two sessions, two ids, each the id of the session it wrote).

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0404-smoke-2026-09-23.md`): one kiso T5 leg on the packed 0.40.4
candidate. Validity and every gate green — verify pass, 29 decisions all
bypass-approved, 0 compactions, none of the three notices, one session
and one trace. `cost_weighted` **31,397.4** over 37 requests, 0 unknown:
**IN BAND** (20,907.0–36,624.0, the range 0.40.3 was read against). Raw
leg: kiso-doc `bench-archives/rel-0404-smoke.tgz`, sha256
`9ee41e11…8b9592d6`.

**The live integration** (the same ruling, part 2) — filled in after the
owner's profiles are switched.

## Evidence

- `npm run check` on the branch — filled in with the counts.
- CI `check` green on the review PR and the release PR.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass — after the publish

### The owner's pass

`bench/rd1/0.40.4-dogfood-checklist.md`.
