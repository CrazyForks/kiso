# 0.41.1 — release report (CANDIDATE)

One fix PR (#132) and one release PR, merged through the protected-branch
flow with the required `check` green. The owner's word, 2026-09-24:
"release 0.41.1; once it is green, release".

- **Tag** `v0.41.1` (annotated) on `main` — filled in at publish.
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.41.0`.
- **Lockstep:** fix-only → a PATCH.

## What is in it

The reviewer's four items after 0.41.0 (the owner's GPT session read the
round and approved it, asking for these before uooki adopts the service):

| PR | what changed |
|---|---|
| #132 | **one store by construction:** an open session's replay and `events()` read the session's own log — the object the run writes; a factory that binds its agents to a different store is DETECTED: persist-first puts a run's terminal on disk before it is yielded, so a settled run whose terminal the service's store does not hold proves a second store — that run's `done` rejects with `StoreMismatchError` and the session is refused from then on |
| #132 | **the store's ownership:** the host owns it; `service.close()` aborts runs and forgets sessions and never closes the store — the host calls `store.closeAll()` at process end (JSDoc, README) |
| #132 | **`sanitizeToolArgs` walks nested arguments:** prose keys stripped and long strings cut at every depth, nesting capped with a marker; documented as a UI-noise filter, not a privacy boundary, with the `sanitize` seam for a product that needs one |
| #132 | `docs/architecture.md`: "No remote stack IN THE RUNTIME" pointing at §1b's optional packages; the composition paragraph names `create-coding-agent.ts` and `coding-prompt.ts` |

## Bench (BM-1 §3)

Everything is in `packages/server` and `docs/`; kiso-code imports none
of it, and no request byte moves (the byte fixture blocks). The release
smoke is the non-regression half on the release artifact.

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0411-smoke-2026-09-24.md`): one kiso T5 leg on the packed 0.41.1
candidate. Validity and every gate green — verify pass (wall 62 s), 28
decisions all bypass-approved, 0 compactions, none of the four notices,
one session and one trace; the trace's 46 lines all validate (36 request
records at schema 7, no request failed, nothing credential-shaped).
`cost_weighted` **35,172.0** over 36 requests, 0 unknown: **inside the rc
range (20,907.0–36,624.0) — IN BAND.** On 0410-F1: two legs on
byte-identical kiso-code bytes now read 19,425 (0.41.0) and 35,172
(0.41.1) — the run-to-run variance the finding named, observed; it closes
as variance. Raw leg: kiso-doc `bench-archives/rel-0411-smoke.tgz`,
sha256 `b9f2d309…6eb49e14`.

## Evidence

- `npm run check` on #132's head: unit 367 files / 3,522 tests, PTY 183
  / 853, the packed smoke and the demo PASS (exit 0); the three
  packages' suites 36 tests incl. the two-store detection and the nested
  sanitizer.
- CI `check` green on #132 and the release PR.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass — after the publish

_filled in_

### The owner's pass

`bench/rd1/0.41.1-dogfood-checklist.md`.
