# 0.40.5 — release report (PUBLISHED, APPROVED)

Two review PRs and one release PR, merged through the protected-branch
flow with the required `check` green. The owner's word, in the worker's
session on 2026-09-23: "yes — merge, then release 0.40.5".

- **Tag** `v0.40.5` (annotated) on `main` `bba5dcfd`.
- **Published** from the 15 tarballs the release smoke ran on: a repack of
  the merged `main` was byte-identical to them (15 sha256, compared).
- **Established on the write path:** a second publish of each of the 15
  was refused as already published (15/15).
- **The owner's machine runs the PUBLISHED artifact:** the 15 packages
  installed globally at 0.40.5 (installed on the 16th attempt, 20 s apart,
  while the registry's read side caught up); `kiso --version` → 0.40.5;
  all 15 read back from disk; `/status` in a plain `kiso` ends with
  `version 0.40.5`.
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.40.4`.

## What is in it

| PR | what changed |
|---|---|
| #112 | a session says when the kiso on disk is not the kiso it runs: after a turn, once per installed version, `✦ kiso <installed> is installed — this session runs <running>; exit and resume it (kiso resume <id>) to use it`; `/status` names both versions. The owner's 2026-09-23 case: a session started on 0.40.2 kept refusing overlapping hunks after 0.40.3 was installed, while `kiso --version` elsewhere said 0.40.3 |
| #113 | a concise README in both editions (EN 538 → 180 lines, ZH 326 → 181): what kiso is and six highlights, then install, models, everyday use, modes, sessions, extending, the SDK; the detailed sections moved verbatim into docs; every README claim gate green unchanged |

## Bench (BM-1 §3)

A CLI display round and a docs round: no request bytes and no durable
events move, so the blockers are the CLI's gates (the PTY suite with the
new end-to-end, pipe identity, the README claim gates).

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0405-smoke-2026-09-23.md`): one kiso T5 leg on the packed 0.40.5
candidate. Validity and every gate green — verify pass, 28 decisions all
bypass-approved, 0 compactions, none of the four notices (the new version
notice silent with the installed kiso the running one), one session and
one trace. `cost_weighted` **42,642.8** over 36 requests, 0 unknown: above
the rc range (20,907.0–36,624.0), inside the widest corrected-text range
(≤ 50,997) — **"proposed, for the reviewer", not a stop**. 0.40.5 changes
no request bytes; against 0.40.4's leg the model thought 2.4× as much
(19,846 thinking characters against 8,141) and wrote 11,857 output tokens
against 6,162 — the cost rode the reasoning, not the product. Raw leg:
kiso-doc `bench-archives/rel-0405-smoke.tgz`, sha256 `ce9aaa50…92683680`.

## Evidence

- `npm run check` on #112: unit 357 files / 3,468 tests, PTY 174 / 818,
  packed smoke and demo PASS; #113: the README gates and the five
  README-reading test files locally, the full chain in CI.
- CI `check` green on #112, #113 and the release PR #114.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass (published 0.40.5, a real model)

- Plain `kiso` — no launcher: the owner's config now sets `mode: bypass`
  and names profile `ds`, whose key is the stored DeepSeek credential.
  Session `dogfood-0405` on a fresh copy of the ledger fixture
  (`/private/tmp/dogfood-0405`).
- The same task as 0.40.3 and 0.40.4: 13 tool calls over 7 requests, 14 s,
  `fresh 2.5k out 1.5k · cache 91%`; `npm test` 4/4; three `edit_file`
  calls, all applied first time; every decision `mode:bypass`.
- `/status` ended with `version 0.40.5`, and no version notice appeared —
  the installed kiso is the running one.

### The owner's pass

`bench/rd1/0.40.5-dogfood-checklist.md`. The owner ran it, in the worker's
session on 2026-09-23: "I did the dogfood for both 0.40.4 and 0.40.5".

## Status: APPROVED (2026-09-23)

Both dogfood passes are in the record. The smoke's "proposed, for the
reviewer" reading (42,642.8, the model's longer reasoning) went to the
owner with the release; the owner raised no objection.
