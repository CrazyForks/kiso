# 0.42.2 — release report (CANDIDATE)

One fix PR (#150) and one release PR, merged through the protected-branch
flow with the required `check` green. The owner's word, 2026-09-25:
"open it" for the products' request #17.

- **Tag** `v0.42.2` (annotated) on `main` — filled in at publish.
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.42.1`.
- **Lockstep:** fix-only → a PATCH.

## What is in it

| PR | what changed |
|---|---|
| #150 | **#17 (uooki, billing):** `provider-openai` had `cacheWrite: null` hardcoded at both usage sites — true of the first-party API, false of a gateway on the same route, which reports `prompt_tokens_details.cache_write_tokens`. The adapter reads it when present (absent stays null; `inputTokens` stays the raw prompt count). **The accounting boundary follows, or a written token is billed twice:** on the "total" convention canonical input is now prompt − cached − written; the trace guard's `freshInput` and the two bench diagnostics use the same expression; the "fresh" route is untouched. `total = input + cacheRead + cacheWrite` holds by construction |

Also on this main since 0.42.1: #146, the kernel headroom refactor
(0.43.0 step 1: `emit()` and `decidedEvent()` in `loop.ts`, zero
behavior, 2,199 → 2,179 lines; verified by the outside reviewer before
the merge).

## Bench (BM-1 §3)

A PROVIDER round: the contract half blocks and is green (the adapter
rig: reported write → `cacheWrite`, absent → null, reported 0 kept; the
canonical tests: 100/30/20 → input 50, the sum is the prompt count). The
LIVE half — a gateway that reports the write — cannot run on this
machine; it is the first product's cold-turn reconciliation on 0.42.2:

_to be recorded here when uooki reports it_

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0422-smoke-2026-09-25.md`): one kiso T5 leg on the packed 0.42.2
candidate (18 tarballs from the release branch's bump commit, every
package rebuilt from the merged main first, installed isolated, sha256
recorded before the leg). Validity and every gate green — verify pass
(wall 60 s), 26 decisions all bypass-approved, 0 compactions, none of
the four notices, one session and one trace; the trace's 42 lines all
validate (32 request records at schema 7, no request failed, nothing
credential-shaped); **every request record and usage event carries
`cacheWrite: null`** — on the first-party route the fix invents no write
and the arithmetic reduces to 0.42.1's, so this leg is the
non-regression half only, as the frozen text says. `cost_weighted`
**26,015.2** over 32 requests, 0 unknown: **inside the rc range
(20,907.0–36,624.0) — IN BAND** (0.42.1: 26,317.4 over 32). Raw leg:
kiso-doc `bench-archives/rel-0422-smoke.tgz`, sha256 `b96c6902…9698bd0`.

## Evidence

- `npm run check` on #150's head `ad50b16e`: unit 369 files / 3,541
  tests, PTY 183 / 853, the packed smoke and the demo PASS (exit 0) —
  the exit read before the PR was opened.
- CI `check` green on #146, #150 and the release PR.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass — after the publish

_filled in_

### The owner's pass

`bench/rd1/0.42.2-dogfood-checklist.md`.
