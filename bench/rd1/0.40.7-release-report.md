# 0.40.7 — release report (PUBLISHED, APPROVED)

Three review PRs and one release PR, merged through the protected-branch
flow with the required `check` green. The owner's word, in the worker's
session on 2026-09-23: "the 0.40.7 batch can go ahead; release it together
when done".

- **Tag** `v0.40.7` (annotated) on `main` `1d6d8690`.
- **Published** from the 15 tarballs the release smoke ran on: a repack of
  the release branch and a repack of the merged `main` were each
  byte-identical to them (15 sha256, compared).
- **Established on the write path:** a second publish of each of the 15
  was refused as already published (15/15).
- **The owner's machine runs the PUBLISHED artifact:** the 15 packages
  installed globally at 0.40.7 (installed on the 31st attempt, 20 s apart,
  while the registry's read side caught up); `kiso --version` → 0.40.7;
  all 15 read back from disk.
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.40.6`.

## What is in it

| PR | what changed |
|---|---|
| #121 | finding 0406-F1: the typed `/model` list names the credential that pays — `stored key` when auth.json pays, the env var only when it does |
| #122 | `/model` says what a subscription sign-in can still do: an expired-but-renewable token reads `oauth, expired — renews on use`; a renewal the token endpoint REFUSED (400/401) is remembered, never re-sent, and makes the profile `(unavailable)` naming `kiso login <provider>`; a network failure marks nothing |
| #126 | CS-1: a delegate evaluator runs only when the user listed it (`"evaluators": [...]`, by real path) — a model-chosen path could be an interpreter running code the child wrote; the tool's description is byte-identical |
| #126 | RO-F4: one credential set for the file tools and the shell — `.npmrc`, `.pypirc`, `.git-credentials`, `.htpasswd`, and `.aws/credentials` / `.config/gh/hosts.yml` / `.docker/config.json` / `.kube/config` by path (the owner's ruling; `.npmrc` was a deliberate omission before) |
| #126 | an `{"error": …}` frame inside a 200 stream is a transport failure, retried — it ended the run as `unknown` (a gateway losing its upstream mid-answer); a frame that says the context is too long keeps that verdict |
| #126 | a killed kiso's merge directories (`kiso-mcp-*`, `kiso-skills-*`) are removed at the next startup — the owner's pid in the name, a live session's never touched, legacy names after a week |
| #126 | trace schema 7: a `provider_error` record carries `providerError { code, status?, message }`, the message capped and redacted |
| #126 | MP-1: `/model` and `/mode` open on the session's own row; a switch that moves the bill adds `paid by … from the next turn — was …` |

## Bench (BM-1 §3)

No request bytes move: CS-1 kept the `delegate` description byte-identical
by design, and no other change touches a prompt, a tool schema, compaction
or context. The provider surface (the error frame) and the trace schema
are covered by their contract tests, which block. The release smoke is the
non-regression half on the release artifact.

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0407-smoke-2026-09-23.md`): one kiso T5 leg on the packed 0.40.7
candidate. Validity and every gate green — verify pass, 25 decisions all
bypass-approved, 0 compactions, none of the four notices, one session and
one trace; the trace's 43 lines all validate (33 request records at
schema 7, no request failed, nothing credential-shaped).
`cost_weighted` **28,060.8** over 33 requests, 0 unknown: **inside the rc
range (20,907.0–36,624.0) — IN BAND.** Raw leg: kiso-doc
`bench-archives/rel-0407-smoke.tgz`, sha256 `79d8c409…5f64779a2b`.

## Evidence

- `npm run check` on the review PR's head: unit 360 files / 3,482
  tests, PTY 183 / 853, the packed smoke (5 consumer tiers) and the demo
  PASS.
- CI `check` green on #121, #122, #126 and the release PR #127.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass (published 0.40.7, a real model)

- Plain `kiso`, profile `ds` (the stored DeepSeek credential; the four
  key env vars unset), mode `bypass`. Session `dogfood-0407` on a fresh
  copy of the ledger fixture (`/private/tmp/dogfood-0407`).
- The same task as 0.40.3–0.40.6: `npm test` 4/4 in 21 s, 19 tool calls
  over 13 requests, `fresh 4.2k out 2.7k · cache 94%`; every decision
  `mode:bypass`; `/status` ends with `version 0.40.7`.
- Six `edit_file` calls, two refused, both the model's own shape and both
  recovered at the next call: one sent an extra property in `edits[1]`
  (schema validation); one put its replacement text into `search`, and
  the refusal quoted what the file actually holds. 0.40.7 changes no
  edit tool; 0.40.3–0.40.6 ran the task in three edits each.
- The typed `/model` list: `ds`, `co` and `op` read `stored key
  (available)` (the gateways through their `kiso login --endpoint` keys),
  `chatgpt` / `sol` read `oauth, expired — renews on use (available)`.

### The owner's pass

`bench/rd1/0.40.7-dogfood-checklist.md`. The owner ran it, in the worker's
session on 2026-09-24: "I did the 0.40.7 dogfood, no problems".

## Status: APPROVED (2026-09-24)

Both dogfood passes are in the record.
