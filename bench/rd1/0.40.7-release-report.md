# 0.40.7 — release report (CANDIDATE)

Three review PRs and one release PR, merged through the protected-branch
flow with the required `check` green. The owner's word, in the worker's
session on 2026-09-23: "the 0.40.7 batch can go ahead; release it together
when done".

- **Tag** `v0.40.7` (annotated) on `main` — filled in at publish.
- **Rollback:** `npm install -g @vincemakes/kiso-code@0.40.6`.

## What is in it

| PR | what changed |
|---|---|
| #121 | finding 0406-F1: the typed `/model` list names the credential that pays — `stored key` when auth.json pays, the env var only when it does |
| #122 | `/model` says what a subscription sign-in can still do: an expired-but-renewable token reads `oauth, expired — renews on use`; a renewal the token endpoint REFUSED (400/401) is remembered, never re-sent, and makes the profile `(unavailable)` naming `kiso login <provider>`; a network failure marks nothing |
| #126 | CS-1: a delegate evaluator runs only when the user listed it (`"evaluators": [...]`, by real path) — a model-chosen path could be an interpreter running code the child wrote; the tool's description is byte-identical |
| #126 | RO-F4: one credential set for the file tools and the shell — `.npmrc`, `.pypirc`, `.git-credentials`, `.htpasswd`, and `.aws/credentials` / `.config/gh/hosts.yml` / `.docker/config.json` / `.kube/config` by path (the owner's ruling; `.npmrc` was a deliberate omission before) |
| #126 | an `{"error": …}` frame inside a 200 stream is a transport failure, retried — it ended the run as `unknown` (a gateway losing its upstream mid-answer); a frame that says the context is too long keeps that verdict |
| #126 | a killed kiso's merge directories (`kiso-mcp-*`, `kiso-skills-*`) are removed at the next startup — the owner's pid in the name, a live session's never touched, legacy names after a week |
| #126 | trace schema 7: a `provider_error` record carries `providerError { code, status?, message }`, the message capped and redacted |
| #126 | MP-1: `/model` and `/mode` open on the session's own row; a switch that moves the bill adds `paid by … from the next turn — was …` |

## Bench (BM-1 §3)

No request bytes move: CS-1 kept the `delegate` description byte-identical
by design, and no other change touches a prompt, a tool schema, compaction
or context. The provider surface (the error frame) and the trace schema
are covered by their contract tests, which block. The release smoke is the
non-regression half on the release artifact.

**The release smoke** (criteria frozen before the run, kiso-doc
`ruling-0407-smoke-2026-09-23.md`): SMOKE_RESULT

## Evidence

- `npm run check` on the review PR's head: CHECK_LINE
- CI `check` green on #121, #122, #126 and the release PR.

## Dogfood (the release rule: both passes before APPROVED)

### The agent's pass — after the publish

### The owner's pass

`bench/rd1/0.40.7-dogfood-checklist.md`.
