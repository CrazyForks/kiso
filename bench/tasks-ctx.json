[
 "This repository has a TypeScript source tree under src/. Over the next several turns you will build answers.json: one entry per TOP-LEVEL exported function in that tree. An entry is {\"file\": path relative to src/, \"name\": the function name, \"start\": the 1-based line its `export function` line is on, \"end\": the 1-based line of the `}` that closes its body at column 0, \"purpose\": one sentence in your own words}. PROTOCOL, and it is part of the task: read each file IN FULL with the read_file tool before recording anything from it, and do not extract these with a shell pipeline or a script — the purpose sentence requires having read the code, and the rest must come from the same reading. Start by listing every .ts file under src/ so you know the full set. Do not read any file yet. Reply with the count.",
 "Batch 1 of 6: read each of these files in full, then append its exported functions to answers.json (create it if absent; it is a JSON array). The files are src/cells/approval-panel.ts, src/cells/components.ts, src/cells/diff.ts, src/cells/ground.ts, src/cells/index.ts.",
 "Batch 2 of 6: read each of these files in full, then append its exported functions to answers.json (create it if absent; it is a JSON array). The files are src/cells/md.ts, src/cells/render.ts, src/cells/strings.ts, src/cells/width.ts, src/tui/approval-panel.ts.",
 "Batch 3 of 6: read each of these files in full, then append its exported functions to answers.json (create it if absent; it is a JSON array). The files are src/tui/ask-panel.ts, src/tui/at-picker.ts, src/tui/components.ts, src/tui/compositor.ts, src/tui/context-ledger.ts.",
 "Batch 4 of 6: read each of these files in full, then append its exported functions to answers.json (create it if absent; it is a JSON array). The files are src/tui/diff.ts, src/tui/editor.ts, src/tui/index.ts, src/tui/lines.ts, src/tui/panel-input.ts.",
 "Batch 5 of 6: read each of these files in full, then append its exported functions to answers.json (create it if absent; it is a JSON array). The files are src/tui/pick-input.ts, src/tui/session-picker.ts, src/tui/status.ts, src/tui/strings.ts, src/tui/transcript.ts.",
 "Batch 6 of 6: read each of these files in full, then append its exported functions to answers.json (create it if absent; it is a JSON array). The files are src/tui/width.ts.",
 "Now finish. First confirm answers.json has an entry for every top-level exported function in every file you read across ALL batches, including batch 1 — if any are missing, recover them and add them. Then, using the entries themselves, find the three functions with the LONGEST bodies (end minus start) and write them to longest.md, naming file, function and length."
]