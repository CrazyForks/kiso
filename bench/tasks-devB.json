["There is a rendering bug in this project. Reported symptom: when `--min` filters rows out of the report, the table is still padded as wide as an owner name that is no longer printed, so the kept rows look indented and the header runs long. Reproduce it, find the cause, and fix it. Read the code before changing it — the symptom is not in the file that causes it. Keep the unfiltered report exactly as it is today: when the long owner IS shown, the table widens for it. `node tests/report.test.js` must still pass. Do not change the tests."]
